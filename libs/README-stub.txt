This folder now includes libxposed-api-stub.jar.

It is a compileOnly stub for IDE/CI builds and is NOT the real LSPosed Modern API library.
It should not be packaged into the APK.
If you later obtain the real libxposed API/service/helper artifacts, keep this stub only as fallback or remove it.
